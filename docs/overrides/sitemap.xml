<?xml version="1.0" encoding="UTF-8"?>
{# Overrides mkdocs/templates/sitemap.xml (Material ships no sitemap of its
   own, so that core template is what would otherwise be used). The page loop
   below is MkDocs' own, kept verbatim so it stays easy to diff against
   upstream; the loop after it adds the machine-readable data endpoints, which
   MkDocs cannot list because it only ever iterates rendered pages. The list
   comes from hooks/sitemap_extras.py — see that file for what's in it, what
   is deliberately left out, and why there's no <lastmod> on these. -#}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for file in pages -%}
    {% if not file.page.is_link and (file.page.abs_url or file.page.canonical_url) %}
    <url>
         <loc>{% if file.page.canonical_url %}{{ file.page.canonical_url|e }}{% else %}{{ file.page.abs_url|e }}{% endif %}</loc>
         {% if file.page.update_date %}<lastmod>{{file.page.update_date}}</lastmod>{% endif %}
    </url>
    {%- endif -%}
{% endfor %}
{%- for url in sitemap_extra_urls | default([]) %}
    <url>
         <loc>{{ url|e }}</loc>
    </url>
{%- endfor %}
</urlset>
